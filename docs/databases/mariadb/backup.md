# MariaDB Backup

Phase 4 prefers `mariadb-dump` for MariaDB sources, falling back to compatible tools only when configured. Restore uses the MariaDB client and follows the same artifact lifecycle as MySQL.
