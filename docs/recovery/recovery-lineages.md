# Recovery Lineages

A lineage represents one native database identity. PostgreSQL uses the system identifier. MySQL and MariaDB use server identity and GTID/binlog continuity. Chains cannot cross lineages.
