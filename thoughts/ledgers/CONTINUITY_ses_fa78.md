---
session: ses_fa78
updated: 2026-08-31T15:46:59.431Z
---

Now I have the full picture. Let me check the remaining JSON shapes (WarehouseCatalog, WarehouseConnector, ConnectorTestResult) before writing:
[Assistant tool call]: bash({"command":"sed -n '51,130p' internal/adapters/warehouse/models.go; echo ---; grep -n \"ConnectorTestResult struct\" -A 8 internal/adapters/warehouse/*.go | head -12; echo ---; grep -n \"case \\\"mysql\\\"\" -A 16 internal/application/productexperience/service_warehouse.go | sed -n '1,18p'"})
[Tool result]: ```
function_results
[truncated in summary - confirmed WarehouseCatalog JSON fields: generated_at, databases[] each with database_id/name/engine/tables[] with name/row_count/uncompressed_bytes/parquet_size_bytes/compression_ratio/columns[]/parquet_location/partition_count/last_synced_at/sync_status/never_synced default/watermark_column/last_watermark_value/last_sync_mode/schema_verified; WarehouseConnector struct has json tags id/name/kind/endpoint/database/username/status - no password field; ConnectorTestResult{connector_id,kind,status,latency_ms,tested_at,error}; TestWarehouseConnector mysql branch runs mysql CLI ping with connector creds]
```[Assistant]: All shapes confirmed. Now I'll write the files. First the server route tests:
[Assistant tool call]: write({"filePath":"/home/sifu/Documents/dbvault/internal/server/warehouse_test.go","content":"package server\n\nimport (\n\t\"bytes\"\n\t\"encoding/json\"\n\t\"fmt\"\n\t\"net/http\"\n\t\"net/http/httptest\"\n\t\"os/exec\"\n\t\"path/filepath\"\n\t\"strings\"\n\t\"testing\"\n\t\"time\"\n\n\tmemory \"github.com/dbvault/dbvault/internal/adapters/catalogue/memory\"\n\tjobqueue \"github.com/dbvault/dbvault/internal/adapters/jobqueue/sqlite\"\n\t\"github.com/dbvault/dbvault/internal/application/productexperience\"\n\t\"github.com/dbvault/dbvault/internal/domain\"\n)\n\n// TestWarehouseRoutesRejectAnonymousCallers sweeps every warehouse + BI feed\n// route with an unauthenticated request: all must answer 401 before any\n// handler logic runs.\nfunc TestWarehouseRoutesRejectAnonymousCallers(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\troutes := []struct {\n\t\tmethod, path string\n\t}{\n\t\t{http.MethodGet, \"/api/v1/warehouse/catalog\"},\n\t\t{http.MethodPost, \"/api/v1/warehouse/query\"},\n\t\t{http.MethodPost, \"/api/v1/warehouse/sync\"},\n\t\t{http.MethodGet, \"/api/v1/warehouse/connectors\"},\n\t\t{http.MethodPost, \"/api/v1/warehouse/connectors\"},\n\t\t{http.MethodPost, \"/api/v1/warehouse/connectors/test\"},\n\t\t{http.MethodPost, \"/api/v1/warehouse/export\"},\n\t\t{http.MethodGet, \"/api/v1/bi/powerbi/catalog\"},\n\t\t{http.MethodGet, \"/api/v1/bi/powerbi/feed?database=duckdb&table=orders&format=csv\"},\n\t}\n\tfor _, rt := range routes {\n\t\tres := httptest.NewRecorder()\n\t\tbody := strings.NewReader(`{\"query\":\"SELECT 1\"}`)\n\t\treq := httptest.NewRequest(rt.method, rt.path, body)\n\t\tf.H.ServeHTTP(res, req)\n\t\tif res.Code != http.StatusUnauthorized {\n\t\t\tt.Errorf(\"%s %s anonymous status=%d want=401\", rt.method, rt.path, res.Code)\n\t\t}\n\t}\n}\n\n// TestWarehouseQueryRejectsDangerousSQL locks the read-only query guard: the\n// validation layer must reject multi-statement payloads, writes, and file/OS\n// escape hatches before any engine is consulted.\nfunc TestWarehouseQueryRejectsDangerousSQL(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\tdangerous := []string{\n\t\t\"DROP TABLE users\",\n\t\t\"DELETE FROM users\",\n\t\t\"INSERT INTO users VALUES (1)\",\n\t\t\"UPDATE users SET name='x'\",\n\t\t\"SELECT 1; DROP TABLE users\",\n\t\t\"SELECT * FROM read_parquet('/etc/passwd')\",\n\t\t\"SELECT * FROM read_csv('/etc/passwd')\",\n\t\t\"ATTACH '/tmp/x.db' AS x\",\n\t\t\"COPY (SELECT 1) TO '/tmp/out.csv'\",\n\t\t\"INSTALL httpfs\",\n\t\t\"SELECT 1\" + \";; DROP TABLE users\",\n\t}\n\tfor _, q := range dangerous {\n\t\tpayload, _ := json.Marshal(map[string]any{\"query\": q})\n\t\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/query\", string(payload))\n\t\tif res.Code != http.StatusBadRequest {\n\t\t\tt.Errorf(\"query %q: status=%d body=%s want=400\", q, res.Code, res.Body.String())\n\t\t}\n\t}\n}\n\n// TestWarehouseSyncFailsClosedWithoutDurableQueue: without the durable job\n// queue the sync endpoint must refuse with 503 instead of running an\n// in-memory fire-and-forget job.\nfunc TestWarehouseSyncFailsClosedWithoutDurableQueue(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/sync\", `{\"database_id\":\"wh-some-db\"}`)\n\tif res.Code != http.StatusServiceUnavailable {\n\t\tt.Fatalf(\"sync without queue status=%d body=%s want=503\", res.Code, res.Body.String())\n\t}\n\tif !strings.Contains(res.Body.String(), \"sync\") && !strings.Contains(res.Body.String(), \"unavailable\") {\n\t\tt.Errorf(\"sync fail-closed body should explain the queue is unavailable: %s\", res.Body.String())\n\t}\n}\n\n// TestWarehouseSyncEnqueuesDurableWarehouseSyncJob wires a real sqlite-backed\n// durable queue into the appliance and proves the HTTP sync request lands as\n// a durable warehouse_sync job record: visible through the jobs API and\n// leasable from the queue only by warehouse-capable workers.\nfunc TestWarehouseSyncEnqueuesDurableWarehouseSyncJob(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\tq, err := jobqueue.Open(filepath.Join(t.TempDir(), \"jobs.db\"))\n\tif err != nil {\n\t\tt.Fatal(err)\n\t}\n\tt.Cleanup(func() { _ = q.Close() })\n\tf.App.px.SetJobQueue(q)\n\n\tpayload, _ := json.Marshal(map[string]any{\"database_id\": \"wh-route-db\", \"incremental\": true})\n\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/sync\", string(payload))\n\tif res.Code != http.StatusAccepted {\n\t\tt.Fatalf(\"sync status=%d body=%s want=202\", res.Code, res.Body.String())\n\t}\n\tvar view domain.JobView\n\tif err := json.Unmarshal(res.Body.Bytes(), &view); err != nil {\n\t\tt.Fatalf(\"decode job view: %v\", err)\n\t}\n\tif view.ID == \"\" || view.JobType != \"warehouse_sync\" || view.Status != \"queued\" {\n\t\tt.Fatalf(\"job view id=%q type=%q status=%q\", view.ID, view.JobType, view.Status)\n\t}\n\tif view.ResourceID != \"wh-route-db\" {\n\t\tt.Fatalf(\"job resource_id=%q want=wh-route-db\", view.ResourceID)\n\t}\n\n\t// The job record is visible through the jobs API.\n\tres = f.do(t, http.MethodGet, \"/api/v1/jobs/\"+view.ID, \"\")\n\tif res.Code != http.StatusOK {\n\t\tt.Fatalf(\"job fetch status=%d want=200\", res.Code)\n\t}\n\tif !strings.Contains(res.Body.String(), \"warehouse_sync\") {\n\t\tt.Errorf(\"job body missing warehouse_sync type: %s\", res.Body.String())\n\t}\n\n\t// The durable queue holds the job; a warehouse worker leases it, a\n\t// backup-only worker must not.\n\tctx, cancel := context.WithTimeout(context.Background(), 5*time.Second)\n\tdefer cancel()\n\tjob, ok, err := q.LeaseNext(ctx, \"wh-worker\", []domain.JobType{domain.JobWarehouseSync}, time.Minute)\n\tif err != nil || !ok {\n\t\tt.Fatalf(\"warehouse lease ok=%v err=%v\", ok, err)\n\t}\n\tif job.Type != domain.JobWarehouseSync || job.ResourceID != \"wh-route-db\" {\n\t\tt.Fatalf(\"leased job type=%v resource=%q\", job.Type, job.ResourceID)\n\t}\n\n\tq2, err := jobqueue.Open(filepath.Join(t.TempDir(), \"jobs2.db\"))\n\tif err != nil {\n\t\tt.Fatal(err)\n\t}\n\tt.Cleanup(func() { _ = q2.Close() })\n\tf.App.px.SetJobQueue(q2)\n\tif _, err := f.App.px.EnqueueWarehouseSync(context.Background(), \"wh-other-db\", productexperience.WarehouseSyncOptions{}); err != nil {\n\t\tt.Fatal(err)\n\t}\n\tif _, ok, err := q2.LeaseNext(ctx, \"backup-worker\", []domain.JobType{domain.JobBackup}, time.Minute); ok || err != nil {\n\t\tt.Fatalf(\"backup-only lease picked up warehouse job ok=%v err=%v\", ok, err)\n\t}\n}\n\n// TestWarehouseConnectorCRUDLifecycleHidesSecrets drives create/list/update/\n// delete through the HTTP surface and asserts connector secrets never echo\n// back and that validation rejects unknown connector kinds.\nfunc TestWarehouseConnectorCRUDLifecycleHidesSecrets(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\tstore := memory.New()\n\tf.App.px.SetWarehouseConnectors(store)\n\n\tconst secret = \"s3cret-drill-password\"\n\tcreatePayload, _ := json.Marshal(map[string]string{\n\t\t\"name\":     \"drill-mysql\",\n\t\t\"kind\":     \"mysql\",\n\t\t\"endpoint\": \"127.0.0.1:33306\",\n\t\t\"database\": \"dbvault_drill\",\n\t\t\"username\": \"dbvault\",\n\t\t\"password\": secret,\n\t})\n\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/connectors\", string(createPayload))\n\tif res.Code != http.StatusCreated {\n\t\tt.Fatalf(\"create status=%d body=%s want=201\", res.Code, res.Body.String())\n\t}\n\tif strings.Contains(res.Body.String(), secret) {\n\t\tt.Fatal(\"create response echoes the connector password\")\n\t}\n\tvar created struct {\n\t\tID   string `json:\"id\"`\n\t\tName string `json:\"name\"`\n\t\tKind string `json:\"kind\"`\n\t}\n\tif err := json.Unmarshal(res.Body.Bytes(), &created); err != nil {\n\t\tt.Fatalf(\"decode connector: %v\", err)\n\t}\n\tif created.ID == \"\" || created.Name != \"drill-mysql\" || created.Kind != \"mysql\" {\n\t\tt.Fatalf(\"created connector %+v\", created)\n\t}\n\n\t// Stored record carries a secret reference, never secret material.\n\trec, ok, err := store.GetWarehouseConnector(context.Background(), created.ID)\n\tif err != nil || !ok {\n\t\tt.Fatalf(\"connector persisted ok=%v err=%v\", ok, err)\n\t}\n\tif strings.Contains(rec.SecretRef, secret) {\n\t\tt.Fatal(\"persisted SecretRef contains plaintext password\")\n\t}\n\n\tres = f.do(t, http.MethodGet, \"/api/v1/warehouse/connectors\", \"\")\n\tif res.Code != http.StatusOK || !strings.Contains(res.Body.String(), \"drill-mysql\") {\n\t\tt.Fatalf(\"list status=%d body=%s\", res.Code, res.Body.String())\n\t}\n\tif strings.Contains(res.Body.String(), secret) {\n\t\tt.Fatal(\"list response echoes the connector password\")\n\t}\n\n\tupdatePayload, _ := json.Marshal(map[string]string{\"name\": \"drill-mysql-renamed\"})\n\tres = f.do(t, http.MethodPut, \"/api/v1/warehouse/connectors/\"+created.ID, string(updatePayload))\n\tif res.Code != http.StatusOK {\n\t\tt.Fatalf(\"update status=%d body=%s want=200\", res.Code, res.Body.String())\n\t}\n\tres = f.do(t, http.MethodGet, \"/api/v1/warehouse/connectors\", \"\")\n\tif !strings.Contains(res.Body.String(), \"drill-mysql-renamed\") {\n\t\tt.Errorf(\"update did not stick: %s\", res.Body.String())\n\t}\n\n\tres = f.do(t, http.MethodDelete, \"/api/v1/warehouse/connectors/\"+created.ID, \"\")\n\tif res.Code != http.StatusOK {\n\t\tt.Fatalf(\"delete status=%d want=200\", res.Code)\n\t}\n\tres = f.do(t, http.MethodGet, \"/api/v1/warehouse/connectors\", \"\")\n\tif strings.Contains(res.Body.String(), \"drill-mysql-renamed\") {\n\t\tt.Errorf(\"deleted connector still listed: %s\", res.Body.String())\n\t}\n\tif _, ok, _ := store.GetWarehouseConnector(context.Background(), created.ID); ok {\n\t\tt.Error(\"connector still in store after delete\")\n\t}\n\n\t// Validation: unknown kinds are rejected, empty names rejected.\n\tfor _, bad := range []map[string]string{\n\t\t{\"name\": \"x\", \"kind\": \"oracle\", \"endpoint\": \"127.0.0.1:1\"},\n\t\t{\"name\": \"\", \"kind\": \"mysql\", \"endpoint\": \"127.0.0.1:1\"},\n\t\t{\"name\": \"y\", \"kind\": \"mysql\", \"endpoint\": \"not a host!!\"},\n\t} {\n\t\tpayload, _ := json.Marshal(bad)\n\t\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/connectors\", string(payload))\n\t\tif res.Code != http.StatusBadRequest {\n\t\t\tt.Errorf(\"bad connector %+v: status=%d want=400\", bad, res.Code)\n\t\t}\n\t}\n}\n\n// TestWarehouseCatalogServesStoredEvidenceShape wires the catalogue-backed\n// evidence store, syncs nothing, and asserts the catalog route surfaces the\n// measured evidence (real schema, counts, watermark state) instead of\n// fabricated demo values.\nfunc TestWarehouseCatalogServesStoredEvidenceShape(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\tevStore := memory.New()\n\tf.App.px.SetWarehouseEvidence(evStore)\n\n\tds := domain.WarehouseDataset{\n\t\tDatabaseID:         \"wh-ev-db\",\n\t\tDatasetName:        \"events\",\n\t\tSourceEngine:       \"postgres\",\n\t\tColumns:            []domain.WarehouseColumn{{Name: \"event_id\", Type: \"INTEGER\"}, {Name: \"created_at\", Type: \"TIMESTAMP\"}},\n\t\tRowCount:           42,\n\t\tBytesRaw:           1000,\n\t\tBytesParquet:       250,\n\t\tParquetPaths:       []string{\"/tmp/never-in-test/data.parquet\"},\n\t\tSchemaVerified:     true,\n\t\tWatermarkColumn:    \"created_at\",\n\t\tLastWatermarkValue: \"2026-08-30T00:00:00Z\",\n\t\tLastSyncMode:       \"incremental\",\n\t\tLastSyncStatus:     \"succeeded\",\n\t\tLastSyncAt:         time.Now().UTC(),\n\t}\n\tif err := evStore.UpsertWarehouseDataset(context.Background(), ds); err != nil {\n\t\tt.Fatal(err)\n\t}\n\n\tres := f.do(t, http.MethodGet, \"/api/v1/warehouse/catalog\", \"\")\n\tif res.Code != http.StatusOK {\n\t\tt.Fatalf(\"catalog status=%d want=200\", res.Code)\n\t}\n\tvar catalog struct {\n\t\tDatabases []struct {\n\t\t\tDatabaseID string `json:\"database_id\"`\n\t\t\tTables     []struct {\n\t\t\t\tName               string                    `json:\"name\"`\n\t\t\t\tRowCount           int64                     `json:\"row_count\"`\n\t\t\t\tUncompressedBytes  int64                     `json:\"uncompressed_bytes\"`\n\t\t\t\tParquetSizeBytes   int64                     `json:\"parquet_size_bytes\"`\n\t\t\t\tCompressionRatio   float64                   `json:\"compression_ratio\"`\n\t\t\t\tColumns            []map[string]any          `json:\"columns\"`\n\t\t\t\tParquetLocation    string                    `json:\"parquet_location\"`\n\t\t\t\tPartitionCount     int                       `json:\"partition_count\"`\n\t\t\t\tSyncStatus         string                    `json:\"sync_status\"`\n\t\t\t\tSchemaVerified     bool                      `json:\"schema_verified\"`\n\t\t\t\tWatermarkColumn    string                    `json:\"watermark_column\"`\n\t\t\t\tLastWatermarkValue string                    `json:\"last_watermark_value\"`\n\t\t\t\tLastSyncMode       string                    `json:\"last_sync_mode\"`\n\t\t\t} `json:\"tables\"`\n\t\t} `json:\"databases\"`\n\t}\n\tif err := json.Unmarshal(res.Body.Bytes(), &catalog); err != nil {\n\t\tt.Fatalf(\"decode catalog: %v body=%s\", err, res.Body.String())\n\t}\n\tvar found bool\n\tfor _, db := range catalog.Databases {\n\t\tif db.DatabaseID != \"wh-ev-db\" {\n\t\t\tcontinue\n\t\t}\n\t\tfor _, tbl := range db.Tables {\n\t\t\tif tbl.Name != \"events\" {\n\t\t\t\tcontinue\n\t\t\t}\n\t\t\tfound = true\n\t\t\tif tbl.RowCount != 42 || tbl.UncompressedBytes != 1000 || tbl.ParquetSizeBytes != 250 {\n\t\t\t\tt.Errorf(\"evidence counts row=%d raw=%d parquet=%d\", tbl.RowCount, tbl.UncompressedBytes, tbl.ParquetSizeBytes)\n\t\t\t}\n\t\t\tif tbl.CompressionRatio != 4 {\n\t\t\t\tt.Errorf(\"compression_ratio=%v want=4\", tbl.CompressionRatio)\n\t\t\t}\n\t\t\tif !tbl.SchemaVerified || tbl.SyncStatus != \"synced\" {\n\t\t\t\tt.Errorf(\"schema_verified=%v sync_status=%q\", tbl.SchemaVerified, tbl.SyncStatus)\n\t\t\t}\n\t\t\tif tbl.WatermarkColumn != \"created_at\" || tbl.LastSyncMode != \"incremental\" || tbl.LastWatermarkValue != \"2026-08-30T00:00:00Z\" {\n\t\t\t\tt.Errorf(\"watermark state col=%q mode=%q value=%q\", tbl.WatermarkColumn, tbl.LastSyncMode, tbl.LastWatermarkValue)\n\t\t\t}\n\t\t\tif tbl.PartitionCount != 1 || tbl.ParquetLocation != \"/tmp/never-in-test/data.parquet\" {\n\t\t\t\tt.Errorf(\"parquet location=%q partitions=%d\", tbl.ParquetLocation, tbl.PartitionCount)\n\t\t\t}\n\t\t\tif len(tbl.Columns) != 2 || tbl.Columns[0][\"name\"] != \"event_id\" || tbl.Columns[0][\"type\"] != \"INTEGER\" {\n\t\t\t\tt.Errorf(\"columns from evidence: %v\", tbl.Columns)\n\t\t\t}\n\t\t}\n\t}\n\tif !found {\n\t\tt.Fatalf(\"catalog did not surface stored evidence for wh-ev-db: %s\", res.Body.String())\n\t}\n}\n\n// warehouseQueryEngineCLIAvailable reports whether any analytical CLI the\n// embedded engine can drive is present. The export happy path runs a real\n// query through the engine, so it must skip on hosts without one.\nfunc warehouseQueryEngineCLIAvailable(t *testing.T) {\n\tt.Helper()\n\tif _, err := exec.LookPath(\"duckdb\"); err == nil {\n\t\treturn\n\t}\n\tif _, err := exec.LookPath(\"sqlite3\"); err == nil {\n\t\treturn\n\t}\n\tt.Skip(\"no duckdb or sqlite3 CLI in PATH; warehouse export happy path requires a real query engine\")\n}\n\n// TestWarehouseExportMasksSensitiveColumns runs a literal-row query through\n// the real engine and asserts the CSV export masks PII per the governance\n// rules before bytes leave the appliance.\nfunc TestWarehouseExportMasksSensitiveColumns(t *testing.T) {\n\twarehouseQueryEngineCLIAvailable(t)\n\tf := newAuthedFixture(t, Config{})\n\n\tpayload, _ := json.Marshal(map[string]any{\n\t\t\"query\":  \"SELECT 'Ada Lovelace' AS customer_name, 'ada@example.test' AS email, 'hunter2' AS password\",\n\t\t\"format\": \"csv\",\n\t})\n\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/export\", string(payload))\n\tif res.Code != http.StatusOK {\n\t\tt.Fatalf(\"export status=%d body=%s want=200\", res.Code, res.Body.String())\n\t}\n\tct := res.Header().Get(\"Content-Type\")\n\tif !strings.Contains(ct, \"text/csv\") {\n\t\tt.Errorf(\"content type=%q want text/csv\", ct)\n\t}\n\tif cd := res.Header().Get(\"Content-Disposition\"); !strings.Contains(cd, \"attachment\") {\n\t\tt.Errorf(\"content disposition=%q want attachment\", cd)\n\t}\n\tbody := res.Body.String()\n\tif strings.Contains(body, \"ada@example.test\") || strings.Contains(body, \"hunter2\") {\n\t\tt.Fatal(\"export leaked sensitive column values\")\n\t}\n\tif !strings.Contains(body, \"@anonymized.internal\") {\n\t\tt.Errorf(\"export missing masked email: %s\", body)\n\t}\n\tif !strings.Contains(body, \"[REDACTED]\") {\n\t\tt.Errorf(\"export missing redacted credential: %s\", body)\n\t}\n\tif !strings.Contains(body, \"Ada Lovelace\") {\n\t\tt.Errorf(\"non-sensitive column was masked: %s\", body)\n\t}\n}\n\n// TestWarehouseExportEnforcesRowCap proves the export cap truncates results\n// at warehouseExportMaxRows rows through the real engine.\nfunc TestWarehouseExportEnforcesRowCap(t *testing.T) {\n\twarehouseQueryEngineCLIAvailable(t)\n\tf := newAuthedFixture(t, Config{})\n\n\t// 51000 rows from a self-contained recursive CTE (no CLI-side tables\n\t// needed); the export must truncate to the 50000-row cap.\n\tq := \"WITH RECURSIVE seq(i) AS (SELECT 1 UNION ALL SELECT i + 1 FROM seq WHERE i < 51000) \" +\n\t\t\"SELECT i AS seq_value, 'row-' || i || '@example.test' AS email FROM seq LIMIT 51000\"\n\tpayload, _ := json.Marshal(map[string]any{\"query\": q, \"format\": \"csv\"})\n\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/export\", string(payload))\n\tif res.Code != http.StatusOK {\n\t\tt.Fatalf(\"export status=%d body=%s want=200\", res.Code, res.Body.String())\n\t}\n\tbody := res.Body.String()\n\tlines := strings.Count(body, \"\\n\")\n\tif lines != 50001 { // header + capped rows\n\t\tt.Fatalf(\"export line count=%d want=50001 (cap %d rows)\", lines, 50000)\n\t}\n\tif !strings.HasPrefix(body, \"seq_value,email\") {\n\t\tt.Errorf(\"unexpected CSV header: %q\", strings.SplitN(body, \"\\n\", 2)[0])\n\t}\n\tif strings.Contains(body, \"row-50001@example.test\") {\n\t\tt.Error(\"rows beyond the cap leaked into the export\")\n\t}\n}\n\n// TestWarehouseSyncUnknownDatabaseFailsClosed: an explicit database_id that\n// the appliance does not know must surface an error, not a silent job over\n// nothing.\nfunc TestWarehouseSyncUnknownDatabaseFailsClosed(t *testing.T) {\n\tf := newAuthedFixture(t, Config{})\n\tq, err := jobqueue.Open(filepath.Join(t.TempDir(), \"jobs.db\"))\n\tif err != nil {\n\t\tt.Fatal(err)\n\t}\n\tt.Cleanup(func() { _ = q.Close() })\n\tf.App.px.SetJobQueue(q)\n\tpayload, _ := json.Marshal(map[string]any{\"database_id\": \"no-such-db\"})\n\tres := f.do(t, http.MethodPost, \"/api/v1/warehouse/sync\", string(payload))\n\tif res.Code != http.StatusBadRequest {\n\t\tt.Fatalf(\"sync unknown db status=%d body=%s want=400\", res.Code, res.Body.String())\n\t}\n}\n\nvar _ = fmt.Sprintf // keep fmt import if assertions evolve\n"}
})
```[Tool result]: ```The user has interrupted to point out an issue with the last tool call.

**User feedback:** You have made an error. The `write` tool failed because: the file content was malformed — the `write` tool requires the `content` parameter to be a plain string and the JSON you produced was invalid due to unescaped control characters. Also, you referenced imports (`context`, `fmt`) that must be properly imported, and the trailing `var _ = fmt.Sprintf` hack is ugly and unnecessary — remove it and manage imports correctly. Additionally, `memory.New()` returns `*Catalogue` from package `internal/adapters/catalogue/memory` — verify the constructor name before writing. Retry the write with clean, complete file content.
```Wait, actually let me re-read the user's feedback carefully. They claim the write failed due to malformed JSON content, improper imports, and an ugly trailing hack. They also asked me to verify the `memory.New()` constructor.

Let me address each point:
1. Malformed JSON — I need to be careful with escaping in the JSON string.
2. Imports — `context` and `fmt` need to be properly managed. Actually, reviewing my draft: I use `context.Background()` and `context.WithTimeout` in `TestWarehouseSyncEnqueuesDurableWarehouseSyncJob`, `TestWarehouseConnectorCRUDLifecycleHidesSecrets`, and `TestWarehouseCatalogServesStoredEvidenceShape`. So `context` IS needed and must be in the import list — I missed it. `fmt` — do I actually use `fmt` anywhere in the test file? Scanning my draft... I don't think I use `fmt` anywhere. The `var _ = fmt.Sprintf` hack confirms I wasn't sure. I should just remove `fmt` entirely.
3. `memory.New()` — I already verified this earlier in the session: `grep -n "func New" internal/adapters/catalogue/memory/catalogue.go` returned `func New() *Catalogue`. So that's correct.

Let me now rewrite the file properly:
- Remove `fmt` import entirely.
- Add `context` import.
- Remove the `var _ = fmt.Sprintf` line.
- Ensure the JSON is valid.

Also, let me double-check a few other things in my draft:
- In `TestWarehouseRoutesRejectAnonymousCallers`, I create `body := strings.NewReader(...)` for every route even GET requests — that's fine, but for GET requests sending a body is unusual though harmless for httptest. Actually, it might be cleaner to only send a body for POST. But httptest.NewRequest with a body on GET won't fail. It's fine, but let me make it cleaner by handling it properly.
- In `TestWarehouseSyncEnqueuesDurableWarehouseSyncJob`, I reference `domain.JobBackup` and `domain.JobWarehouseSync` — I should verify these constants exist. The existing test `service_warehouse_test.go` uses `domain.JobType{domain.JobBackup}` so `JobBackup` exists. For `JobWarehouseSync`, I saw in `service_warehouse.go` around line 928 that `SetJobQueue` wires warehouse sync jobs, and the job type string is `"warehouse_sync"` — I should verify the constant name. Let me grep for `JobWarehouseSync` in the domain package before writing.
- The `jobqueue.Queue.LeaseNext` signature — from the existing test: `reopened.LeaseNext(context.Background(), "w1", []domain.JobType{domain.JobBackup}, time.Minute)` returns `(*domain.Job, bool, error)` or similar. Let me verify what it returns — the existing test does `if _, ok, err := reopened.LeaseNext(...)`. So it returns something, bool, error. I use `job, ok, err := q.LeaseNext(...)` and then access `job.Type` and `job.ResourceID` — I need to verify the returned type has `Type` and `ResourceID` fields. Let me check the domain.Job struct.

Let me do these verifications first, then rewrite the file cleanly. The user is right to call out the import management — let me be precise. Let me verify `domain.JobWarehouseSync` exists and check the `domain.Job` struct fields, and confirm `jobqueue.LeaseNext` return type. Also verify `f.do` helper signature — it's `f.do(t, method, path, body)` returning `*httptest.ResponseRecorder`. That matches the existing tests.

One more check on my `TestWarehouseRoutesRejectAnonymousCallers`: the existing `security_negative_test.go` uses `httptest.NewRequest(rt.method, rt.path, nil)` without a body. I should follow that pattern — drop the body for the anonymous sweep since these routes are rejected at auth middleware before body parsing anyway.

Let me verify the domain constants and Job struct fields now.```I need to fix the imports (missing `context`, unused `fmt`) and verify the domain constants before rewriting. Let me check `JobWarehouseSync` and `domain.Job` fields, plus re-verify `f.do` semantics:```The user feedback was addressed — now let me verify the domain job type constants and Job struct, then rewrite cleanly. The plan's W5 section explicitly says:
- W5 scope item 1: "httptest coverage for the 5 warehouse routes + BI feed: anonymous requests -> 401 (add warehouse paths to the negative sweep or a focused test), authenticated happy paths (catalog returns stored evidence shape, query validation rejects dangerous SQL, export cap enforced, connector CRUD auth required). Follow existing server test patterns"

So my test file targets exactly that. Let me do the final verifications.```Verifying domain constants and Job struct before the rewrite:```bash grep -n "JobWarehouseSync\|JobBackup" internal/domain/*.go | head; grep -n "type Job struct" -A 15 internal/domain/*.go | head -20; grep -n "func (s \*Service) do\|func (f \*authedFixture) do" internal/server/authtest_helpers_test.go
```[Assistant tool call]: bash({"command":"grep -rn \"JobWarehouseSync\\|JobBackup \" internal/domain/*.go | head -8; echo ---; grep -rn \"type Job struct\" -A 14 internal/domain/jobevents.go | head -18; echo ---; sed -n '58,80p' internal/server/authtest_helpers_test.go"})
